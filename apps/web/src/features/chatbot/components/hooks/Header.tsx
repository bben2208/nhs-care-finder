export function Header() {
    return (
      <div style={{ fontWeight: 700, marginBottom: 8 }}>
        Triage helper (demo)
        <div style={{ fontSize: 12, color: "#667" }}>Info only — not medical advice.</div>
      </div>
    );
  }